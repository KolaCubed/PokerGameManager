<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PokerGameManager.ViewModels"
             xmlns:behavior="clr-namespace:PokerGameManager.Behaviors"
             x:Class="PokerGameManager.Views.BlindsPage"
             Title="Blinds Configuration">

    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:BlindsVM x:Key="vm"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Reset" Command="{Binding Source={StaticResource vm}, Path=ResetCommand}"/>
        <ToolbarItem Text="Add" Command="{Binding Source={StaticResource vm}, Path=AddCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ListView ItemsSource="{Binding Source={StaticResource vm}, Path=Blinds}">
            <ListView.Behaviors>
                <behavior:BlindsListViewBehavior/>
            </ListView.Behaviors>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>